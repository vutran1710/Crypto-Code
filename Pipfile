[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
pytest = "*"
loguru = "*"
black = "*"

[dev-packages]

[requires]
python_version = "3.9"

[scripts]
test = "pytest -v -s"
format = "black ."

[pipenv]
allow_prereleases = true
